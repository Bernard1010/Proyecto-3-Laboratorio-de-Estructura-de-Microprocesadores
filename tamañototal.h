#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string.h>
#include <stdlib.h>


using namespace std;

int tamm(string comandos, string Documentoss, string du, string dess, string destinos ) 
{	
	comando = du+Documentos+des+destino;
	char *cstr2 = new char[comando.length() + 1];
	strcpy(cstr2, comando.c_str());
	system(cstr2);
	return 0;
}
